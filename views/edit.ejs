<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>index</title>
    <link rel="stylesheet" href="/stylesheets/styles.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>

    <div class="w-full h-full p-10 bg-zinc-900 text-white ">
        <div class="nav">
            <a class="text-blue-500 mt-4 text-xl" href="/read">show user profiles</a>
        </div>
        <h1 class="text-3xl tracking-tighter mb-5">create user Profiles</h1>
        <form action="/update/<%= user._id %>" method="post">
            <input class="px-5 py-2 border-2 border-zinc-800 rounded-lg bg-transparent" name="name" value="<%= user.name %>" type="text" placeholder="name" />
            <input class="px-5 py-2 border-2 border-zinc-800 rounded-lg bg-transparent" name="email" value="<%= user.email %>" type="email" placeholder="email" />
            <input class="px-5 py-2 border-2 border-zinc-800 rounded-lg bg-transparent" name="image" value="<%= user.image %>" type="text" placeholder="image url" />
            <input class="px-5 py-2 bg-yellow-500 border-2 border-zinc-800 rounded-lg " type="submit" value="Update user" />
        </form>
    </div>
</body>

</html>